{
  "scripts": {
    "build": "npm run build:css && npm run build:dist",
    "build:css": "tailwindcss -i assets/css/input.css -o assets/css/output.css --minify",
    "build:dist": "rm -rf dist && mkdir -p dist/assets/css dist/assets/js dist/blog && cp index.html how-it-works.html about.html contact.html blog.html 404.html staticwebapp.config.json dist/ && cp assets/css/output.css dist/assets/css/ && cp assets/js/analytics.js dist/assets/js/ && cp -r assets/images dist/assets/images && cp -r blog/. dist/blog/",
    "watch": "tailwindcss -i assets/css/input.css -o assets/css/output.css --watch",
    "preview": "npm run build && swa start dist --api-location api"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^2.0.9",
    "tailwindcss": "^3"
  }
}
